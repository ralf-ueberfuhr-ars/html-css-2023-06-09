{
  "name": "html-schulung",
  "version": "1.0.0",
  "description": "",
  "main": "sample.js",
  "scripts": {
    "sample": "tsc && node samples/typescript/sample.js",
    "watch": "tsc -w & sass --watch samples",
    "build": "tsc && sass samples",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.63.6",
    "typescript": "^5.1.5"
  }
}